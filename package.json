{
  "name": "roomdesigner",
  "private": true,
  "scripts": {
    "verify": "npm --prefix backend run build && npm --prefix backend test && npm --prefix frontend run build && npm --prefix frontend test"
  }
}
